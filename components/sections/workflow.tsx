'use client';
import { useState } from 'react';
import { workflow } from '@/data/portfolio';
import { SectionHeading } from '@/components/shared';
import { ArrowRight, Crosshair, Check } from 'lucide-react';
export default function Workflow(){const[step,setStep]=useState(0);return <section className="section shell" id="workflow"><div className="workflow-layout"><div><SectionHeading number="04" label="THE ANALYST MINDSET" title="How I investigate an alert." description="A repeatable process. Room for uncertainty. A conclusion another analyst can follow."/><a className="text-link" href="#investigation">Try the investigation demo<ArrowRight size={16}/></a></div><div className="workflow-interactive"><div className="workflow-steps" role="group" aria-label="Investigation stages">{workflow.map((w,i)=><button key={w.title} className={`workflow-step ${step===i?'active':''}`} onClick={()=>setStep(i)} aria-pressed={step===i} aria-controls="workflow-detail"><span>{String(i+1).padStart(2,'0')}</span>{w.title}{step===i&&<ArrowRight size={15}/>}</button>)}</div><div className="workflow-detail" id="workflow-detail" role="region" aria-live="polite" aria-label="Selected investigation stage"><div className="workflow-detail-top"><Crosshair size={23}/><span>STAGE {String(step+1).padStart(2,'0')} / 10</span></div><h3>{workflow[step].title}</h3><p>{workflow[step].detail}</p><blockquote>“{workflow[step].question}”</blockquote><div className="workflow-artifact"><Check size={15}/><span>{workflow[step].artifact}</span></div></div></div></div></section>}
